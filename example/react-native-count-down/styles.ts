import { PlusStyle } from '@mfe/plus';

export const useStyles = PlusStyle.create({
    countDown: {
        alignItems: 'center',
        justifyContent: 'center',
        textAlign: 'center',
    },
    text: {
        color: '#FF5477',
        fontFamily: 'PingFang SC',
        fontSize: 12,
        fontWeight: '400',
    },
});
